# Capacitor (fase 6) — empaquetar la PWA para tiendas
#
# npm i -D @capacitor/cli
# npm i @capacitor/core @capacitor/android @capacitor/ios
# npx cap init "Tattoo Desk" com.tattoodesk.app --web-dir=out
# npx cap add android && npx cap add ios
#
# Ver capacitor.config.json. La entrega principal hoy es PWA (manifest + sw.js).
